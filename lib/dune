(library
 (name monk)
 (inline_tests)
 (preprocess
  (pps ppx_deriving.show ppx_inline_test ppx_expect)))

(menhir
 (modules parser))

(ocamllex lexer)

(env
 (dev
  (flags
   (:standard -warn-error -A))))
